from setuptools import setup
setup(
   name='fetch_cneuromod',
   version='0.1',
   description='Fetch paths for filenames from cneuromod data',
   author='SIMEXP',
   packages=['.'],  #same as name
   author_email='courtois.neuromod@gmail.com'
)
